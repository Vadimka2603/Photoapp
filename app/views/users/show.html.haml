- provide(:title, @user.name)
- if current_user==@user
  - if current_user
    .row
      %aside.col-md-4
        %section.micropost_form= render 'shared/micropost_form'
        = link_to "Фото с инстаграма", instagram_path
        = link_to "sub", callback_path
  = render 'shared/user_micropost'
- else
  = render 'shared/user_micropost'